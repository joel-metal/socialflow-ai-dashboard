# All values must be provided via a secrets manager (e.g. AWS Secrets Manager +
# External Secrets Operator, Sealed Secrets, or Vault).
# Never commit real secrets to source control.
# This file is an empty skeleton — all values must be set before applying.
apiVersion: v1
kind: Secret
metadata:
  name: socialflow-secrets
type: Opaque
stringData:
  DATABASE_URL: ""
  REDIS_PASSWORD: ""
  JWT_SECRET: ""
  JWT_REFRESH_SECRET: ""
  TWITTER_API_KEY: ""
  TWITTER_API_SECRET: ""
